#include <stdio.h>
#define MAX 10

int main(){
    int n,a[MAX][MAX],visited[MAX]={0},queue[MAX],front=0,rear=0,i,j;
    scanf("%d",&n);
    for(i=0;i<n;i++)
        for(j=0;j<n;j++)
            scanf("%d",&a[i][j]);

    queue[rear++]=0; visited[0]=1;

    while(front<rear){
        int v=queue[front++];
        printf("%d ",v);
        for(i=0;i<n;i++)
            if(a[v][i] && !visited[i]){
                queue[rear++]=i;
                visited[i]=1;
            }
    }
}
